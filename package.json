{
  "name": "snapgrab",
  "version": "1.1.4",
  "description": "SnapGrab - Premium Media Downloader",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "clean": "if exist dist rmdir /s /q dist",
    "build": "npm run clean && electron-builder --win",
    "pack": "npm run clean && electron-builder --dir",
    "release": "npm run clean && electron-builder --win --publish always",
    "push": "git add . && (git commit -m \"update\" || echo \"nothing to commit\") && npm version patch && git push --follow-tags && npm run release"
  },
  "keywords": [],
  "author": "SnapGrab",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^41.2.1",
    "electron-builder": "^26.8.1"
  },
  "build": {
    "appId": "com.snapgrab.app",
    "productName": "SnapGrab",
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "icon": "public/icon.png",
      "signAndEditExecutable": false,
      "artifactName": "SnapGrab.${version}.${ext}",
      "publish": {
        "provider": "github",
        "owner": "shaswatxd",
        "repo": "snapgrab-downloader",
        "releaseType": "release"
      }
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "artifactName": "SnapGrab.Setup.${version}.exe"
    },
    "portable": {
      "artifactName": "SnapGrab.Portable.exe"
    },
    "files": [
      "main.js",
      "preload.js",
      "public/**/*"
    ]
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  }
}
